# Nginx service configuration and management scripts

scripts/
├── nginx-reload.sh      # Graceful reload of Nginx configuration
├── nginx-test.sh        # Test Nginx configuration syntax
├── ssl-renew.sh         # Renew Let's Encrypt certificates
└── nginx-logs.sh        # View and manage Nginx logs